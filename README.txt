#Annotation
Program constructs annotation to the text, counting target sentences.
First of all, it builds histogram of words, using the file «cleaning» as filter.Next, we count target sentences, using our histogram. If you want to use this program, you must change file’s path.